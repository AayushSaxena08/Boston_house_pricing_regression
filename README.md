# Boston House Pricing

Conda create a new environment 

    conda create -p venv python==3.7 -y

Install all libraries required by making a folder `requirements.txt` and running a command in cmd `pip install -r requirements.txt`